1.  HISTSIZE строка 862 количество хранимых в истории команд
    HISTFILESIZE строка 846 макс количество строк в файле истории
2.  Включает в себя одновременно дерективы ignorespace и ignoredups, что означает: не будут сохраняться в истории строки команд, начинающиеся с пробела и строки, совпадающие
    с предыдущей выполненой командой
3.  Используется когда нужно содать несколько файлов touch file{1,2}.txt, папок mkdir {1..10}, либо сделать подстановку значений в скобках куда-либо еще. Строка 746 Brace Expansion
4.  touch {1..100000}.txt 300000 создать не получиться выходит ошибка -bash: /usr/bin/touch: Argument list too long. Превышен лимит аргументов для команды, ограничение наложен операционной системой.
5.  Проверка на существование файла tmp и что он является папкой, если условие выполнено, то возвращает 0, если нет то 1. В данном случае будет 0, так как такая папка существует.
6.  mkdir /tmp/new_path_directory/
    cp /bin/bash /tmp/new_path_directory/
    PATH=/tmp/new_path_directory/:$PATH
    type -a bash
7.  at - команда выполниться в указанное время
    batch - команда выполниться когда средняя загрузка системы упадет ниже 1.5, либо если в atd указано другое значение этого порога 
